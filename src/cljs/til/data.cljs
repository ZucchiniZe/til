(ns til.data)

(def initial-db {:current-route :home
                 :userid nil
                 :current-id 1
                 :route {:id nil
                         :tag nil
                         :not-found nil}
                 :tils []})
